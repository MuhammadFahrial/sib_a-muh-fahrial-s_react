Nama : A Muh Fahrial S
Program : React and React Native Basic
Kode : RCTN-KS05-012

Summary Live Session Sesi 3

javascript pada dasarnya termasuk kedalam bahasa pemrograman berorientasi objek objek. javascipt adalah bahasa OOP yang bersifat prototype-based.
cara mendefinisikan class di ES6. pertama kita gunakan sintaks class kemudian di lanjutkan dengan nama class yang ingin di buat. biasanya nama class di mulai dengan huruf kapital. kemudian di dalamnya bisa di definisikan methods, contructor ataupun properties.

Mengelola data dan state terkadang butuh inisialisasi logika di awal pembuatan atau inisialisasi object. kita bisa menggunakan method khusus yang namanya constructor seperti mendeklarasikan method, bedanya contructor secara otomatis akan di jalankan pada saat inisialisasi class di dalam contructor ini kita bisa mendeklarasikan properties yang akan di gunakan di dalam class keyword this.

Tiga pilar utama dalam pemrograman berorientasi object adalah encapsulation, polymorphism dan inherintance. ES6 menyediakan sintaks yang mudah untuk membuat hubungan parent child untuk class yang di buat menggambarkan sebuah class merupakan turunan dari class lain atau dengan kata lain inherintance

Adakalanya kita menurunkan sebuah class tapi kita butuh berinteraksi dengan class parebt dari class anaknya tentu saja cara mendapatkan nama dari class tersebut kita definisikan properti baru, tapi kadi redundan, kodenya jadi berulang. kita bisa menggunakan keyword super yang dapat menghubungkan kita dengan leluhur dari class tersebut.

Sebelum ES6, ada beberapa cara yang dapat di tempuh untuk membuat kode javascript kita lebih modular. pertama, kita bisa menggunakan IIFE atau immediately-invoked function expression, sebuah fungsi yang di bungkus tanda kurung langsung di eksekusi. Kemudian muncul CommonJS, selanjutnya ada AMD atau require.js dan terakhir ES6 yang menggunakan sintaks import dan export

Promise adalah sebuah objek yang menghasilkan sebuah nilai di masa yang akan datang. kemungkinan hasil dari promise hanya ada 2: resolve atau reject. cara penggunaan promise cukup sederhana, kita bisa merangkai fungsi promise dengan method .then() untuk resolve dan .method.catch() untuk rejection. dengan syarat fungsi, method atau library harus sudah mendukung promise misalnya saja fungsi fetch() yang sudah tersedia di berbagai browser modern saat ini

fitur baru javascript yang namanya async/await. kita sudah dapat menjalankan kode asynchronous dengan gaya synchronous. dan kompatibel dengan promise, jadi semua fungsi atau method yang sudah mendukung promise yang dapat dijalankan dengan async/await dengan syarat browser atau nodejs support dengan async/await dan hanya berjalan di dalam fungsi karena harus menandai mana fungsi yang akan menjalankan kode async dan mana fungsi biasa
