import React from "react";
import { Outlet } from "react-router-dom";

const MeetUp = () => {
  return (
    <>
      <div>MeetUp</div>
      <Outlet />
    </>
  );
};

export default MeetUp;
